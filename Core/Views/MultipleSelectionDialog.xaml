<Window x:Class="GadgetTools.Core.Views.MultipleSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Multiple Selection" Height="400" Width="350"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="CanResize"
        MinHeight="300" MinWidth="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 説明テキスト -->
        <TextBlock Grid.Row="0" Text="Select items and use checkboxes. Right-click or use Delete key to remove items."
                   FontSize="11" Foreground="#666" Margin="0,0,0,8" TextWrapping="Wrap"/>
        
        <!-- 検索ボックス -->
        <TextBox Grid.Row="1" x:Name="SearchBox" 
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 ToolTip="Type to filter items..."
                 Margin="0,0,0,10"/>
        
        <!-- 追加ボックス -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="NewItemBox" 
                     Text="{Binding NewItemText}"
                     ToolTip="Enter new item name"
                     KeyDown="NewItemBox_KeyDown"/>
            <Button Grid.Column="1" Content="Add" Width="50" Margin="5,0,0,0"
                    Click="AddItem_Click" IsDefault="False"/>
        </Grid>
        
        <!-- 選択リスト -->
        <ListBox Grid.Row="3" x:Name="ItemsListBox"
                 ItemsSource="{Binding FilteredItems}"
                 SelectionMode="Extended"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 KeyDown="ItemsListBox_KeyDown"
                 SelectionChanged="ItemsListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <CheckBox Grid.Column="0" Content="{Binding Name}" 
                                  IsChecked="{Binding IsSelected}"
                                  Margin="2"/>
                        
                        <Button Grid.Column="1" Content="✕" Width="20" Height="20"
                                Margin="5,0,0,0" FontSize="10" 
                                ToolTip="Delete this item"
                                Click="DeleteItem_Click"
                                Tag="{Binding}"
                                Background="Transparent" 
                                BorderThickness="0"
                                Foreground="#CC0000"
                                Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Opacity" Value="0.3"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="1.0"/>
                                            <Setter Property="Background" Value="#FFEBEE"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Delete Selected" Click="DeleteSelected_Click" 
                              InputGestureText="Del"/>
                    <Separator/>
                    <MenuItem Header="Select All" Click="SelectAllFromContext_Click"/>
                    <MenuItem Header="Deselect All" Click="DeselectAllFromContext_Click"/>
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>
        
        <!-- 選択状態表示 -->
        <TextBlock Grid.Row="4" 
                   Text="{Binding SelectionSummary}" 
                   Margin="0,10,0,10"
                   FontWeight="Medium"/>
        
        <!-- ボタン -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Delete Selected" Width="100" Margin="0,0,10,0"
                    Click="DeleteSelected_Click" 
                    Background="#FFCDD2" Foreground="#B71C1C"
                    ToolTip="Delete selected items from list (Del key)"/>
            <Button Content="Select All" Width="80" Margin="0,0,10,0"
                    Click="SelectAll_Click"/>
            <Button Content="Clear All" Width="80" Margin="0,0,10,0"
                    Click="ClearAll_Click"/>
            <Button Content="OK" Width="80" Margin="0,0,10,0"
                    IsDefault="True" Click="OK_Click"/>
            <Button Content="Cancel" Width="80" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>