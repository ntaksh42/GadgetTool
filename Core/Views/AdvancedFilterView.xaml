<UserControl x:Class="GadgetTools.Core.Views.AdvancedFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <Style x:Key="FilterButton" TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="MinWidth" Value="60"/>
        </Style>
        
        <Style x:Key="ConditionPanel" TargetType="Border">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- タイトルバー -->
        <Border Grid.Row="0" Background="#007ACC" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Advanced Filter" FontSize="14" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                <Button Content="✕" Background="Transparent" Foreground="White" BorderThickness="0" 
                        Padding="8,4" Margin="10,0,0,0" HorizontalAlignment="Right"
                        Click="CloseButton_Click" Cursor="Hand"/>
            </StackPanel>
        </Border>
        
        <!-- ツールバー -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button Content="➕ Add Condition" Command="{Binding AddConditionCommand}" Style="{StaticResource FilterButton}"/>
            <Button Content="💾 Save Filter" Command="{Binding SaveFilterCommand}" Style="{StaticResource FilterButton}"/>
            <ComboBox ItemsSource="{Binding SavedFilters}" SelectedItem="{Binding SelectedSavedFilter}" 
                      Width="150" Margin="10,0" ToolTip="Load saved filter" DisplayMemberPath="Name"/>
            <Button Content="🗑️ Clear All" Command="{Binding ClearAllCommand}" Style="{StaticResource FilterButton}"/>
            <TextBlock Text="{Binding FilterStatusText}" VerticalAlignment="Center" Margin="15,0,0,0" 
                       FontStyle="Italic" Foreground="#666"/>
        </StackPanel>
        
        <!-- フィルタ条件リスト -->
        <ScrollViewer Grid.Row="2" Margin="10,0,10,10">
            <ItemsControl ItemsSource="{Binding FilterConditions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ConditionPanel}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 論理演算子 -->
                                <ComboBox Grid.Column="0" SelectedIndex="{Binding LogicalOperator}" Margin="0,0,8,0">
                                    <ComboBoxItem Content="AND"/>
                                    <ComboBoxItem Content="OR"/>
                                </ComboBox>
                                
                                <!-- フィールド選択 -->
                                <ComboBox Grid.Column="1" ItemsSource="{Binding DataContext.AvailableFields, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          SelectedValue="{Binding Field}" DisplayMemberPath="DisplayName" SelectedValuePath="Name"
                                          Margin="0,0,8,0"/>
                                
                                <!-- 演算子選択 -->
                                <ComboBox Grid.Column="2" SelectedIndex="{Binding Operator}" Margin="0,0,8,0">
                                    <ComboBoxItem Content="Contains"/>
                                    <ComboBoxItem Content="Equals"/>
                                    <ComboBoxItem Content="Not Equals"/>
                                    <ComboBoxItem Content="Starts With"/>
                                    <ComboBoxItem Content="Ends With"/>
                                    <ComboBoxItem Content="Greater Than"/>
                                    <ComboBoxItem Content="Less Than"/>
                                </ComboBox>
                                
                                <!-- 値 -->
                                <TextBox Grid.Column="3" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                                
                                <!-- 削除ボタン -->
                                <Button Grid.Column="4" Content="🗑️" ToolTip="Remove condition"
                                        Command="{Binding DataContext.RemoveConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Width="30" Height="25"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- ボタンパネル -->
        <Border Grid.Row="3" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,1,0,0" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Apply Filter" Command="{Binding ApplyFilterCommand}" 
                        Style="{StaticResource FilterButton}" IsDefault="True"/>
                <Button Content="Reset" Command="{Binding ResetFilterCommand}" 
                        Style="{StaticResource FilterButton}"/>
                <Button Content="Cancel" Click="CancelButton_Click" 
                        Style="{StaticResource FilterButton}" IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>