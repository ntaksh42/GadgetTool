<Window x:Class="GadgetTools.Core.Views.SaveQueryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="クエリを保存" Height="300" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="💾 クエリを保存"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,20"
                   Foreground="#2B579A"/>

        <!-- Query Name -->
        <Label Grid.Row="1" Content="クエリ名 *" Target="{Binding ElementName=QueryNameTextBox}"/>
        <TextBox Grid.Row="2" 
                 x:Name="QueryNameTextBox"
                 Text="{Binding QueryName, UpdateSourceTrigger=PropertyChanged}"
                 MaxLength="100"/>

        <!-- Description -->
        <Label Grid.Row="3" Content="説明（オプション）" Target="{Binding ElementName=DescriptionTextBox}"/>
        <TextBox Grid.Row="4" 
                 x:Name="DescriptionTextBox"
                 Text="{Binding QueryDescription, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 MaxLength="500"
                 Height="80"
                 VerticalScrollBarVisibility="Auto"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="保存" 
                    Click="SaveButton_Click"
                    IsDefault="True"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderBrush="#4CAF50"/>
            <Button Content="キャンセル" 
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>